import styled from "styled-components";

const CenterContainer = styled.div`
  background-color: #1f961f;
  flex: 1;
`;

function Center() {
  return <CenterContainer>Center</CenterContainer>;
}

export default Center;
